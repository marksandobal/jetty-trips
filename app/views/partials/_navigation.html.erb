<nav class="navbar navbar-expand-lg navbar-light nav-color">
<%= link_to "Jeety trips", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if user_signed_in? %>
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Viajes", trips_index_path, class: "nav-link" %>
        </li>
      </ul>
      <% end %>
      <ul class="navbar-nav session-option">
      <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <% if user_signed_in? %>
            <%= current_user.email %>
            <% else %>
            Nueva Sesión
            <% end %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
              <%= link_to "Cerrar Sesión",destroy_user_session_path,class: "dropdown-item",method: :delete %>
            <% else %>
              <%= link_to "Iniciar Sessión", new_user_session_path, class: "dropdown-item" %>
              <%= link_to "Nuevo Registro", new_user_registration_path, class: "dropdown-item" %>
            <% end %>
          </div>
      </li>
      <li></li>
      </ul>
  </div>
</nav>
<br>